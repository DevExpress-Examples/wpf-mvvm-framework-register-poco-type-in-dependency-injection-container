<UserControl x:Class="MicrosoftDI.DetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext="{Binding Path=DetailViewModel, Source={StaticResource ServiceLocator}}">

    <UserControl.Resources>
        <Style x:Key="BaseStyle" TargetType="{x:Type Control}">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Width" Value="120"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource BaseStyle}"/>
        <Style TargetType="Button" BasedOn="{StaticResource BaseStyle}"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding Item.FirstName}"
                 IsEnabled="{Binding IsEnabled, ElementName=updateButton}"/>
        <TextBox Grid.Column="1"
                 Text="{Binding Item.LastName}"
                 IsEnabled="{Binding IsEnabled, ElementName=updateButton}"/>
        <Button Grid.Column="3"
                Name="updateButton"
                Content="Update"
                Command="{Binding UpdateCommand}"/>
    </Grid>
</UserControl>
