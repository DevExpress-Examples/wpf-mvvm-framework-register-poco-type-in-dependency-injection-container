<Window x:Class="AutofacDI.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AutofacDI"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding Path=MainViewModel, Source={StaticResource ServiceLocator}}">

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0.15*"/>
        </Grid.RowDefinitions>
        <GroupBox Header="Main View">
            <ListBox BorderThickness="0" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" DisplayMemberPath="DisplayText"/>
        </GroupBox>
        <GroupBox Header="Detail View" Grid.Row="1">
            <local:DetailView DataContext="{Binding Detail}"/>
        </GroupBox>
    </Grid>
</Window>
